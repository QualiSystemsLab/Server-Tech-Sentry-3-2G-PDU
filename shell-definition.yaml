tosca_definitions_version: tosca_simple_yaml_1_0

metadata:
  template_name: Sentry3G2Pdu
  template_author: ksaper, upd. tims@quali.com
  template_version: 1.0.0
  template_icon: shell-icon.png

description: >
  TOSCA based PDU shell

imports:
  - cloudshell_standard: cloudshell_pdu_standard_2_0_1.yaml

node_types:

  vendor.switch.Sentry3G2Pdu:
    derived_from: cloudshell.nodes.PDU
    properties:
    #  my_property:
    #    type: string          # optional values: string, integer, float, boolean, cloudshell.datatypes.Password
    #    default: fast
    #    description: Some attribute description
    #    constraints:
    #      - valid_values: [fast, slow]
      NIC Serial Number:
        type: string
        description: Factory Issued Serial Number of the device
        tags: [setting, read_only]
      System Version:
        type: string
        description: OS/Firmware version on the device
        tags: [setting, read_only]
    capabilities:
      auto_discovery_capability:
        type: cloudshell.capabilities.AutoDiscovery
        properties:        
          enable_auto_discovery:
            type: boolean
            default: true
          auto_discovery_description:
            type: string
            default: Describe the auto discovery
          inventory_description:
            type: string
            default: Describe the resource shell template
          User:
            type: string
            default: admn
          Password:
            type: cloudshell.datatypes.Password
            default: admn
          SNMP Version:
            type: string
            default: v2c
          SNMP Read Community:
            type: string
            default: public
          SNMP Write Community:
            type: string
            default: private
          SNMP V3 User:
            type: string
          SNMP V3 Private Key:
            type: string
          SNMP V3 Password:
            type: cloudshell.datatypes.Password
    artifacts:
      icon:
        file: shell-icon.png
        type: tosca.artifacts.File
      driver:
        file: Sentry3G2PduDriver.zip
        type: tosca.artifacts.File
